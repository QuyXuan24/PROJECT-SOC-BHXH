# =========================================================
# DATABASE CONFIG (Cấu hình cơ sở dữ liệu)
# =========================================================
# Mật khẩu Admin cho Postgres - Các thành viên tự đặt ở máy mình
DB_PASSWORD=admin_password_123

# =========================================================
# SECURITY CONFIG (Cấu hình an ninh)
# =========================================================
# Key mã hóa AES cho hồ sơ BHXH (Phải đúng 32 ký tự cho AES-256)
AES_KEY=32_characters_secret_key_bhxh_soc

# Chìa khóa để tạo mã Token đăng nhập (JWT)
JWT_SECRET=super_secret_key_for_auth_tokens

# =========================================================
# SYSTEM CONFIG (Cấu hình hệ thống)
# =========================================================
# Cổng chạy Backend (Mặc định: 5000)
PORT=5000

# Cổng chạy Frontend (Mặc định: 3000)
FRONTEND_URL=http://localhost:3000